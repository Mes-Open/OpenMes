/**
 * @openmes/ui — universal exports (resolve to .web.* under Vite, .native.* under Metro).
 * Native-only patterns (sheets, snackbar, app bars…) live in '@openmes/ui/native'.
 */
export * from './tokens';
