import { ViewTemplatesScreen } from '@/screens/(drawer)/admin/view-templates/index';

/** View Templates — line dashboard column presets (REST). */
export default function ViewTemplatesPage() {
  return <ViewTemplatesScreen />;
}
