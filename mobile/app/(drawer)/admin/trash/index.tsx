import { AdminTrashScreen } from '@/screens/(drawer)/admin/trash/index';

/** Trash — soft-deleted records, restorable in place (REST). */
export default function AdminTrashPage() {
  return <AdminTrashScreen />;
}
