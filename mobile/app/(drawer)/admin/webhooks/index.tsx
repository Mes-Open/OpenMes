import { AdminWebhooksScreen } from '@/screens/(drawer)/admin/webhooks/index';

/** Webhooks — outgoing endpoint list (REST). */
export default function AdminWebhooksPage() {
  return <AdminWebhooksScreen />;
}
