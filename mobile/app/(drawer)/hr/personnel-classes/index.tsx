import { PersonnelClassesList } from '@/screens/hr/personnel-classes';

/** Personnel Classes — admin HR config list (REST). */
export default function PersonnelClassesPage() {
  return <PersonnelClassesList />;
}
